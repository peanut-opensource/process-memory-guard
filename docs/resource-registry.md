# Resource registry

| Resource ID | Purpose / environment | Owner / source | Host | Port | Database / schema / namespace | Version or service type | Credential reference | Data source / freshness | Health check | Allowed fallback |
|---|---|---|---|---|---|---|---|---|---|---|
| `RES-LOCAL-PROCTABLE-01` | Read local process identity and physical footprint; local macOS | macOS kernel via `libproc` | Current Mac only | N/A | Process table | macOS `libproc` process APIs | Current user permissions; no secret | Live kernel data, refreshed at the configured interval | App self-test must enumerate processes successfully | None |
| `RES-TARGET-REMOTEPAIRINGD-01` | Monitored Apple pairing service; local macOS | Apple system installation | Current Mac only | N/A | Bundle ID `com.apple.CoreDevice.remotepairingd` | Apple-signed per-user XPC service | N/A | Exact path and Apple code signature validated at app start | `SecStaticCodeCheckValidity` with `anchor apple` | None; an invalid path/signature stops monitoring |
| `RES-MONITORED-EXECUTABLES-USER-01` | Additional process rules explicitly selected in the UI; local macOS | User-selected installed applications/executables | Current Mac only | N/A | Per-rule UUID namespace persisted in `groupedConfigurationV2` | Exact executable path plus macOS designated code-signing requirement | Current user permissions; no secret | Live process data; identity is captured when added and revalidated whenever a matching process runs | Exact path match followed by `SecStaticCodeCheckValidity` | None; an identity mismatch blocks that rule rather than switching targets |
| `RES-AUTO-DISCOVERY-01` | Task-manager-style live overview of all readable processes; local macOS | Current user-visible process table via `libproc` | Current Mac only | N/A | Ephemeral live discovery keyed by exact executable path | Grouped `proc_pidpath` + `ri_phys_footprint` observations; up to 200 rows shown in UI | Current user permissions; no secret | Refreshed at the configured interval; not persisted as monitoring rules until promoted | Process table enumeration succeeds; each displayed path and PID is live at last sample | None; unreadable processes remain absent and never become guessed rules |
| `RES-MACOS-NOTIFY-01` | Passive abnormal-memory alerts; local macOS | macOS Notification Center | Current Mac only | N/A | Bundle notifications | UserNotifications framework | User notification permission; no secret | Generated only on threshold crossing or reminder cooldown | Notification authorization status | Menu-bar status icon remains visible if permission is denied |
| `RES-MACOS-MEMORY-PRESSURE-01` | Raise incident severity when the host is under memory pressure; local macOS | macOS kernel via Dispatch memory-pressure source | Current Mac only | N/A | Host memory-pressure events | `DispatchSourceMemoryPressure` normal/warning/critical states | Current user permissions; no secret | Live event-driven state for the current boot session | Dashboard shows the latest pressure state; process thresholds remain authoritative | None; an unavailable pressure signal stays unknown and does not change threshold decisions |
| `RES-OPENAI-RESPONSES-01` | Optional AI second-opinion analysis; user-enabled only | OpenAI API | `api.openai.com` | `443/TLS` | `/v1/responses` | Responses API with Structured Outputs | macOS Keychain item `com.local.RemotePairingGuard.openai` / `api-key`; key is never stored in project files | Sends only target identity, threshold, interval, and up to 10 memory samples when an anomaly first crosses the local threshold | Explicit “Test AI connection” action; disabled by default | No provider fallback; deterministic local monitoring and alerts continue without AI |
| `RES-GITHUB-PEANUT-PMG-01` | Public source repository; production/open-source | `peanut-opensource` GitHub organization | `github.com` | `443/TLS` for `gh` API and Git transport | Repository `peanut-opensource/process-memory-guard`, default branch `main` | GitHub Git repository | Existing GitHub CLI credential from `gh auth status`; no token stored in project | Local `main` is authoritative before push; remote freshness checked after push by exact commit SHA | `gh repo view peanut-opensource/process-memory-guard` and compare `main` commit | None |
| `RES-LOCAL-APP-INSTALL-01` | Installed menu-bar application; local macOS | This repository's signed build artifact | Current Mac | N/A | `/Applications/Process Memory Guard.app` | macOS application bundle version `2.1.1` | Current user filesystem authorization; no secret | Rebuilt from the same commit before install | Bundle version, code-signature verification, launch plus process presence | Existing installation is backed up before replacement; no alternate install path |

Target executable:

`/Library/Apple/System/Library/PrivateFrameworks/RemotePairing.framework/Versions/A/XPCServices/remotepairingd.xpc/Contents/MacOS/remotepairingd`

No local listener, database, cache, queue, container, domain, or managed local port is used by this project. The only network resource is the optional, explicitly enabled OpenAI endpoint registered above.
